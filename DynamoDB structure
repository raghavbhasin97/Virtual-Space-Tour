celestials {name (PRIMARY KEY), index)}

items {name (PRIMARY KEY)), index, info}

moons {name (PRIMARY KEY)), info}

objects {name (PRIMARY KEY)),info, index, size, weight}

planets {name (PRIMARY KEY)), distance, index, info, moons, period, rotation, size, weight}

states {sessionID (PRIMARY KEY)), name}
